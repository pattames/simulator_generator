[project]
name = "simulator_generator"
version = "1.0.0"
requires-python = ">=3.14.4"
dependencies = ["chainlit>=2.11.0", "pydantic>=2.13.3", "groq>=1.2.0", "anthropic>=0.97.0", "python-dotenv>=1.2.2"]

[project.optional-dependencies]
dev = [
		"ruff", "mypy", "tombi", "pip-tools"
]
